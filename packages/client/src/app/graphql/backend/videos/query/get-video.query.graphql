query getVideo($videoId: String!) {
    getVideo(id: $videoId) {
        id
        name
        channelId
        description
        preview
        likes
        views
        dislikes
        comments {
            content
            createdAt
            userId
        }
        createdAt
    }
}
