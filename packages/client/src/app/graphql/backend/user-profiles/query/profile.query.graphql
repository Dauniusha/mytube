query getProfile($userId: String, $username: String) {
    getUserProfile(userId: $userId, username: $username) {
        email
        lastName
        firstName
        username
        avatar
    }
}
