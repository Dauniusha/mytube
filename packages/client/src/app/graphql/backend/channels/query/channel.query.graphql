query getChannel($ownerId: String, $alias: String, $id: String) {
    getChannel(ownerId: $ownerId, alias: $alias, id: $id) {
        id
        owner
        name
        alias
        description
        avatar
        createdAt
    }
}
