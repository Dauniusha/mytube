query getChannel($ownerId: String, $alias: String) {
    getChannel(ownerId: $ownerId, alias: $alias) {
        id
        owner
        name
        alias
        description
        avatar
        createdAt
    }
}
